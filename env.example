# 环境变量配置示例文件
# 
# 使用方法：
# 1. 复制此文件为 .env.local（在项目根目录）
# 2. 将下面的 your_api_key_here 替换为你的真实 API KEY
# 3. .env.local 文件会被 git 忽略，不会上传到 GitHub
#
# 注意：.env.local 文件需要创建在项目根目录，和 package.json 同级

GEMINI_API_KEY=your_api_key_here
